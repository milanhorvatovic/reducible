{
  "name": "reducible-toolchain",
  "private": true,
  "description": "Pins the Node-based formatter and linter this repository's hooks and CI run. Not a published package; the versions live here so one file pins them and Dependabot sees them.",
  "devDependencies": {
    "markdownlint-cli2": "0.23.2",
    "prettier": "3.9.6"
  },
  "scripts": {
    "format": "prettier --write \"**/*.md\"",
    "format:check": "prettier --check \"**/*.md\"",
    "lint:md": "markdownlint-cli2 \"**/*.md\""
  }
}
